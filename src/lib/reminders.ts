export async function sendReminder7DaysBefore() {
  // Placeholder for future scheduled automation.
  // Intended trigger: daily cron scans booked leads whose eventDate is 7 days away.
  return { active: false };
}

export async function sendReminder1DayBefore() {
  // Placeholder for future scheduled automation.
  // Intended trigger: daily cron scans booked leads whose eventDate is 1 day away.
  return { active: false };
}

